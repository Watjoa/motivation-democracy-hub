# Cross-national measurement invariance (skeleton)
# packages: lavaan, semTools

# model <- 'Need =~ aut1 + aut2 + comp1 + comp2 + rel1 + rel2'
# fit_config <- lavaan::cfa(model, data=df, group="country")
# fit_metric <- lavaan::cfa(model, data=df, group="country", group.equal="loadings")
# semTools::compareFit(fit_config, fit_metric)
