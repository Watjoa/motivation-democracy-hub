---
title: "Explore"
---

Below are exploratory summaries (aggregated). Unit-level data access requires a Data Transfer Agreement (DTA).

```{r}
#| message: false
# library(ggplot2); library(dplyr); library(ggrepel)
# Example (replace with real data):
# df <- data.frame(country=c("BE","NL","FI"), need_sat=c(5.2,5.5,5.8), wellbeing=c(6.5,6.7,7.0))
# ggplot(df, aes(need_sat, wellbeing, label = country)) + geom_point() #+ ggrepel::geom_text_repel()
```
